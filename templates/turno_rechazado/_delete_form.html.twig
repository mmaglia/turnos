<form method="post" action="{{ path('turno_rechazado_delete', {'id': turno_rechazado.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ turno_rechazado.id) }}">
    <button class="btn shadow">Delete</button>
</form>
